spring:
  datasource:
    url: jdbc:mysql://localhost:3306/url_shortener_db
    username: root
    password: Chipheo2k3@

  data:
    redis:
      host: localhost
      port: 6379

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect